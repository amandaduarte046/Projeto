body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

header {
    background-color: #4CAF50;
    color: white;
    padding: 1rem;
}

nav ul {
    list-style-type: none;
    padding: 0;
}

nav ul li {
    display: inline;
    margin-right: 20px;
}

section {
    padding: 20px;
}

#produtos {
    display: flex;
    flex-wrap: wrap; /* Permite que os produtos se organizem em múltiplas linhas */
    justify-content: left; /* Alinha os produtos à esquerda */
}

.produto {
    border: 1px solid #ccc;
    padding: 15px;
    margin: 15px;
    text-align: center; /* Centraliza o texto e os botões */
    width: 200px; /* Define uma largura fixa para cada produto */
    transition: transform 0.3s, box-shadow 0.3s; /* Animação suave ao passar o mouse */
}

.produto:hover {
    transform: scale(1.05); /* Aumenta ligeiramente o tamanho do produto */
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3); /* Adiciona sombra ao passar o mouse */
}

.imagem-produto {
    max-width: 100%; /* Assegura que a imagem não ultrapasse a largura do produto */
    height: auto; /* Mantém a proporção da imagem */
}

footer {
    text-align: center;
    padding: 1rem;
    background-color: #4CAF50;
    color: white;
}
